---
name: motor-safety
description: AGENTS.md §8.1 电机与功率器件保护全文 + 本工程既有电机安全事实。编辑 hc-team/driver/motor/**、driver/step_motor/**、app/tasks/speed_loop/**、app/tasks/platform_2d/** 或任何涉及直流电机、步进电机、舵机、TB6612、H 桥、MOSFET、继电器、PWM、占空比、换向的代码前必须加载。Use before writing or modifying any motor, stepper, servo, H-bridge, PWM, or power-device code.
---

# 电机与功率器件保护（AGENTS.md §8.1）

> **同步义务**：本文正文第一节逐字来自根 `AGENTS.md` §8.1（唯一权威）。修改 AGENTS.md §8.1
> 必须同步本文件，反之亦然；两处不一致时以 AGENTS.md 为准并立即修正本文件。

## §8.1 全文

涉及直流电机、步进电机、舵机、TB6612、H 桥、MOSFET、继电器或其他可能损坏硬件的执行器时，硬件保护是功能的一部分，不得省略。任何电机代码在首次输出前必须确认 PWM 频率、有效极性、方向映射、计数周期、占空比单位和硬件允许范围，禁止根据函数名或旧代码猜测这些参数。

电机控制必须满足：

- 上电、复位、初始化未完成、任务退出和错误状态下，默认输出必须为停止或硬件定义的安全态。
- 速度、占空比、频率、加速度和位置命令必须在唯一负责层做明确限幅；不得在多个层重复限幅并产生不一致结果。
- 输出变化必须有符合机构能力的斜率或加速度限制，禁止单周期从大正值跳到大负值。
- 正反转切换必须先降到零，确认进入停止区间，等待硬件所需死区后再反向。不得通过快速交替方向制造脉冲震荡。
- H 桥输入组合、刹车、滑行、待机和使能顺序必须依据实际芯片与接线定义；禁止生成可能造成桥臂直通的组合。
- 周期控制必须检测命令或反馈是否过期；超时后必须停止输出，不得保持最后一次大功率命令。
- 必须提供简单、确定且可从正常控制流调用的停止接口。保护动作不得依赖调试器暂停程序。
- 修改 PID、编码器方向、PWM、定时器时钟、任务周期或电机接线定义时，必须重新审查完整电机链路，不能只验证被修改的模块。

首次硬件验证必须分阶段进行：先断开负载或架空机构，以低占空比、低速度和短持续时间确认方向、PWM 频率及停止行为，再逐步增加输出。必须使用串口、OLED、示波器、逻辑分析仪或直接可观察的硬件行为留下验证结果；不得把连接调试器或单步观察作为验收方式。

若无法确认电机参数、方向、保护时序或安全输出，必须停止并询问，不得生成可直接驱动硬件的猜测代码。

## 本工程既有事实（改动前先核对，不要重复实现）

- `driver/motor/motor.c` 状态机**已实现** slew 限速、换向过零 + 5ms 死区、100ms 命令超时归零
  （V12 closed，主机 7 项测试覆盖）。不要在上层再做第二份同类保护。
- 左右 PWM 已统一 80 MHz / period 7999 = 10 kHz，单源 `board.syscfg`（V11 closed）；
  `motor_hw.c` 是 Motor 模块唯一 TI 头位置。
- 刹车已收窄为私有 `motor_brake_one`——两轮同属一个运动体，只刹一轮会把车甩转；
  公共面只有 `Motor_BrakeAll`（P9.T2）。
- 步进链路速度限幅唯一所有者是 `driver/step_motor/emm42.c`（RPM≤100、×10 协议尺度）；
  `stepmotor_bus.c` 与调用点不得再做第二次限幅或尺度换算。
- §15 裁定生效中：电机 Driver 的验收 = 主机测试 + 构建 + 依赖扫描；带载/硬件行为验证由用户自理。
